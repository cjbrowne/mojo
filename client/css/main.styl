@import "reset.styl"

header_background = #333
body_background = #CCC
body_foreground = #000
header_foreground = #CCF
input_background = #335
input_foreground = #999

#header
	width 100vw
	height 10vh
	background-color header_background
	padding 1em
	display flex
	align-items center
	flex-flow row
	justify-content flex-end
	color header_foreground
#login
	width 20vw
	padding 1em
	margin 1em
	flex 0 1 auto
	order 1
	.loginField
		width 100%
		border solid 1px header_background
		background-color input_background
		color input_foreground
		&:focus
			outline none
			border-color lighten(input_background,50%)
			box-shadow 0 0 5px lighten(input_background,50%)
		placeholder((font-style italic) (text-align center))
	.loginButton
		float right
		clear none
		width 50%
#content
	background-color body_background
	height 90vh
	width 100vw
#register
	width 50vw
	padding 1em
	margin 0 auto
	display flex
	flex-flow column
	.registerField
		width 70%
		border solid 1px body_background
		background-color input_background
		color input_foreground
		align-self center
		&:focus
			outline none
			border-color lighten(input_background,50%)
			box-shadow 0 0 5px lighten(input_background,50%)
		placeholder((font-style italic) (text-align center))
	.registerButton
		width 20%
		align-self flex-end

#languageBar
	flex 0 1 auto
	order 0
	font-family 'Arial', 'Helvetica', sans-serif
	font-size 18pt
	display flex
	flex-flow column
	align-items center
	.label
		flex 0 1 auto
		order 0
	#languageChooser
		flex 0 1 auto
		order 1
		text-align center
		background-color input_background
		color input_foreground
		option
			text-align center // some day, maybe...
			font-size 12pt